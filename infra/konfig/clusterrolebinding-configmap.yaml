apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: konfig-configmap-access
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: konfig-configmap-access
subjects:
  - kind: ServiceAccount
    name: konfig
    namespace: konfig-system
