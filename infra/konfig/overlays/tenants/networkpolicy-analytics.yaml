# GENERATED by generate.py from registry.yaml — DO NOT EDIT.
# Regenerate: python3 infra/konfig/overlays/tenants/generate.py
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: konfig-tenant-egress-analytics
  namespace: analytics
  labels:
    app.kubernetes.io/managed-by: konfig-tenant-overlay
    konfig.io/tenant: analytics
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/part-of: analytics
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          k8s-app: kube-dns
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: konfig-system
      podSelector:
        matchLabels:
          app: konfig
    ports:
    - protocol: TCP
      port: 50051
