load("//bazel/oci:defs.bzl", "konfig_oci_image")

konfig_oci_image(
    name = "konfig",
    binary = "//rust/konfig:konfig_bin",
    binary_name = "konfig",
    exposed_ports = [
        "50051/tcp",
        "9090/tcp",
    ],
    repository = "kasa288/konfig",
)
