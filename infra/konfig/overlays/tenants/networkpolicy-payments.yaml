# GENERATED by generate.py from registry.yaml — DO NOT EDIT.
# Regenerate: python3 infra/konfig/overlays/tenants/generate.py
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: konfig-tenant-egress-payments
  namespace: payments
  labels:
    app.kubernetes.io/managed-by: konfig-tenant-overlay
    konfig.io/tenant: payments
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/part-of: payments
  policyTypes:
  - Egress
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: kube-system
      podSelector:
        matchLabels:
          k8s-app: kube-dns
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: konfig-system
      podSelector:
        matchLabels:
          app: konfig
    ports:
    - protocol: TCP
      port: 50051
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: data
      podSelector:
        matchLabels:
          app: postgres
    ports:
    - protocol: TCP
      port: 5432
